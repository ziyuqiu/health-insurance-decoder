<p><%=form_tag(add_plan_path, controller:"users", action:"add_plan", remote:true) do%>
    <%=collection_select :plan, :name, Plan.all, :id, :name%>
    <%= submit_tag("Add a New Plan", class:"mdl-button mdl-js-button mdl-button--raised mdl-js-ripple-effect")%>
<%end%></p>
